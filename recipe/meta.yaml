{% set name = "badger-opt" %}
{% set version = "0.9.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/badger-opt-{{ version }}.tar.gz
  sha256: 6ed72ff984b4106e27b1cd0105aaf2de1595517e64da7e23c15cf96fbaf98448

build:
  number: 0
  noarch: python
  entry_points:
    - badger = badger.__main__:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - coolname
    - pandas
    - pillow
    - pyqt
    - pyqtgraph
    - python >=3.6
    - pyyaml
    - qdarkstyle

test:
  imports:
    - badger
    - badger.actions
  commands:
    - pip check
    - badger --help
  requires:
    - pip

about:
  home: https://github.com/SLAC-ML/Badger
  summary: Core of the Badger optimizer
  license: GPL-3.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - wenatuhs
